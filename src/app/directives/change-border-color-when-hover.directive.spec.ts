import { ChangeBorderColorWhenHoverDirective } from './change-border-color-when-hover.directive';

describe('ChangeBorderColorWhenHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeBorderColorWhenHoverDirective();
    expect(directive).toBeTruthy();
  });
});
